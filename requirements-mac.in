# Top-level desktop dependencies (macOS, Apple Silicon). Pinned to the
# known-good versions; the full transitive tree + hashes are locked into
# requirements-mac.txt via:  uv pip compile requirements-mac.in --generate-hashes
PyQt6==6.11.0
pyobjc-framework-Cocoa==12.1
pillow==12.2.0
tomli==2.4.1
py2app==0.28.10
parakeet-mlx==0.5.1
# Speaker diarization for transcript speaker labels (optional at runtime —
# the app degrades to channel-level labels without it).
sherpa-onnx==1.13.4
# Local executive summaries after each transcript (Qwen 3.5 4B via MLX).
mlx-lm==0.31.3

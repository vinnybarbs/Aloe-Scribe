.venv/
__pycache__/
*.pyc
*.wav
!tests/sample.wav
/tmp/
.DS_Store
*.log
build/
dist/
*.egg-info/
assets/AppIcon.icns
assets/icon.ico
installer/Output/
bin/aloe-audio-capture

# User settings — created from config.toml.example on install. Untracked so a
# user's calendar URL / output folder never collides with an update.
config/config.toml

# Meeting transcripts the app drops in the repo root during dev runs. These
# are private work notes and must never reach the public repo.
/20*-*.md

# Parakeet model weights — fetched from GitHub Release at install time
models/
.agents/
developer-id.csr
